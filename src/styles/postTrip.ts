import { StyleSheet } from 'react-native';

export const postTripStyles = StyleSheet.create({
	content: {
		flex: 1,
		display: 'flex',
		gap: '2%',
	},
	submitButton: {
		marginTop: '5%',
	},
});

export default postTripStyles;
